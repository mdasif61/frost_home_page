import logo from '../assets/logo.png'

const SideBar = () => {
    return (
        <div className='bg-black'>
            <img src={logo} alt="logo"/>
        </div>
    );
};

export default SideBar;